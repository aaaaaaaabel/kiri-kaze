# Favicon & OG Image

Place these files in this directory (project root `public/`):

- **favicon.svg** — Site favicon (browser tab icon, SVG)
- **og-image.png** — Open Graph image for social sharing (recommended: 1200×630 px)

These are referenced in `nuxt.config.ts` under `app.head`.
